seed: 20260426
num_envs: 2
num_updates: 1
rollout_steps: 8
num_minibatches: 2

device: cpu
log_dir: runs/ppo
run_name: ppo_smoke
save_interval: 1
log_interval: 1

world_yaml_base_path: configs/default.yaml

hidden_dim: 32
num_layers: 1
lr: 0.0003
max_grad_norm: 0.5

gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
entropy_coef: 0.02
value_coef: 0.5
ppo_epochs: 1

belief_on: true
belief_dim: 16
belief_checkpoint_path: runs/belief/test_belief_model.pt
belief_q_on: true
belief_terminal_on: false
use_tensorboard: false
